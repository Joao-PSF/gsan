# Segurança — Riscos Identificados (Fase 0)

Achados do diagnóstico inicial. Nenhuma correção aplicada. Interpretação das prioridades (revisada em 2026-08-13 — este projeto **não opera** nenhuma instalação GSAN): **P0\*** = obrigatório em qualquer instalação GSAN operante e pré-condição no checklist da migração futura (não é atividade de execução deste projeto); **P1** = requisito de nascimento do SISAN, antes do primeiro deploy acessível (incluindo a VPS); **P2** = durante o desenvolvimento dos módulos.

| # | Achado | Evidência | Risco | Prioridade / ação |
| - | ------ | --------- | ----- | ----------------- |
| 1 | Senha de usuário com **SHA-1 sem salt** | `gcom/util/Criptografia.java:17` (`MessageDigest.getInstance("SHA")`), usado por `validarUsuario` via `FiltroUsuario.SENHA`; `usur_nmsenha varchar(40)` | Quebra trivial por rainbow table em caso de vazamento | P1: novo sistema autentica contra hash legado e re-hasheia no primeiro login com BCrypt/Argon2 (coluna nova); política de expiração acelera a troca |
| 1b | Token MD5 efêmero em servlets auxiliares | `gcom/util/Util.java` (MD5) em `AcessarOperacionalServlet:48` e `AcessarNovoBatchServlet:98` | MD5 como token de acesso; **não participa do login** | P1: substituir por token com escopo e expiração — ver [`modulos/seguranca.md`](../modulos/seguranca.md) |
| 2 | Credenciais de banco padrão versionadas (senha = login) | `gsan-migracoes/.../20160118183208_create_roles.sql` | Acesso direto ao banco se exposto na rede | P0\*: em instalações operantes, rotacionar todas as roles `gsan_*` e verificar `pg_hba.conf`/exposição de rede (checklist de migração). No SISAN: credencial padrão proibida (verificação automatizável no pipeline); estas credenciais jamais são reutilizadas |
| 3 | API de pagamento com "autenticação" que não autentica o chamador | `gcom/api/pagamentocredito/PagamentoCreditoFilter.java:18-33` — compara `req.getRequestURL()` (**host do próprio servidor**, não a origem) com dois hosts `http://` hard-coded; o filtro **não bloqueia**, apenas marca `request.setAttribute("filtro", 403)` e segue; a recusa ocorre em `PagamentoCreditoAPI:160,169-170` | Qualquer cliente que alcance o host permitido passa; controle partido entre duas classes; sem TLS | P0\*: em instalações operantes, proxy com TLS + token real (tabela `seguranca.token`) — checklist de migração. No SISAN: APIs nascem com autenticação real e TLS |
| 4 | ~830 pontos de SQL/HQL concatenado | `Repositorio*HBM` (grep de concatenação em WHERE) | SQL Injection | P1/P2: no código novo, 100% parametrizado; no legado, corrigir sob demanda os fluxos expostos a entrada direta do usuário |
| 5 | Ausência de TLS ponta a ponta | domínios `http://` hard-coded; JBoss 4 sem config TLS no repo | Credenciais e dados pessoais em claro | P0\*/P1: em instalações operantes, TLS no proxy reverso (checklist). SISAN: TLS desde a VPS, HSTS e cookies `Secure` |
| 6 | Sessões/cookies sem atributos modernos | `web.xml` sem `HttpOnly`/`Secure`/`SameSite` (Servlet 2.x não suporta) | Sequestro de sessão, CSRF | P1: SISAN com Spring Security (CSRF, fixation, timeout, cookies seguros); em instalações legadas, proxy pode adicionar atributos (checklist) |
| 7 | Stack EOL sem patches (JBoss 4.0.1, Struts 1.1, log4j 1.x, Axis2 1.5.1, commons-httpclient 3, Hibernate 3) | `lib/`, `build.xml` | CVEs conhecidos sem correção disponível | P0\*: em instalações operantes, reduzir exposição de rede (só via proxy) — checklist; a mitigação definitiva é a própria modernização |
| 8 | Dados pessoais (LGPD) em base e logs | Clientes com CPF/NIS, `consulta_receita_federal`, `consulta_cdl`; 70 colunas `bytea` com documentos | Vazamento/uso indevido | P2: anonimização obrigatória em massas de teste; revisão de logs no sistema novo; inventário de dados pessoais por tabela |
| 9 | Upload de arquivos sem validação centralizada | commons-fileupload antigo; múltiplos fluxos (retornos bancários, fotos, leituras) | Upload malicioso | P2: validação de tipo/tamanho/conteúdo centralizada no novo; armazenar fora do webroot |
| 10a | Chave PIX hard-coded no gerador de QR Code (achado 2026-08-14) | `gcom/arrecadacao/GeradorQrCodePIX.java` (`chave = "testqrcode01@bb.com.br"`) | Segredo/identificador de recebimento fixo em código; risco de QR Code apontar para chave indevida | P1: no SISAN, chave PIX é configuração por ambiente/companhia, nunca constante — ver [arrecadacao.md §15](../modulos/arrecadacao.md) |
| 10 | Trilha de auditoria dependente da aplicação | `operacao_efetuada`, `tabela_linha_alteracao` preenchidas via código | Escritas fora da aplicação não auditadas | P2: preservar trilha no novo sistema; avaliar auditoria complementar no banco para tabelas financeiras |

## Achados de 2026-09-14 (revisão externa + rastreio dirigido do fluxo de autorização)

Todos verificados por leitura de código em `HEAD = 2031c4ca`. **Nenhuma execução, nenhuma exploração ofensiva** — apenas análise do fluxo de autorização, conforme a regra do projeto. Nenhum valor de segredo é transcrito.

| # | Achado | Evidência | Risco | Prioridade / ação |
| - | ------ | --------- | ----- | ----------------- |
| 11 | **Segredo versionado: chave de API do provedor de SMS em código-fonte** | `gcom/util/sms/ServicoSMS.java:17` (constante Java) **e** `src/gcom/properties/sms.properties:1` — dois exemplares no Git | Segredo comprometido por definição (está no histórico do repositório). Agravado por `ServicoSMS:35` usar URL **`http://`** hard-coded (o properties traz `https://`, mas `inicializarPropriedades()` nas linhas 72-84 tem o corpo comentado e **retorna `null`**) → a chave trafega em claro | **P0 — rotação obrigatória.** A chave entra na lista de rotação (regra do projeto). No SISAN: segredo em cofre/variável de ambiente, nunca em código nem em properties versionado; TLS obrigatório |
| 12 | **`/api/ordem-servico/*` sem autenticação em endpoints de escrita** | `web.xml:994-1000` mapeia o servlet **sem filtro algum** (`filtroSegurancaAcesso` é `*.do`; `PagamentoCreditoFilter` é `/api/pagamentoCredito/*`). `OrdemServicoAPI:36-58`: `encerrar`, `fotos` e `programadas` não verificam credencial; `validarLogin:107` não emite sessão nem token para as chamadas seguintes; despacho por `getRequestURI().contains(...)` | Encerramento de OS e upload de fotos sem identificação da origem | **P0\*** em instalação operante: bloquear `/api/ordem-servico/*` no proxy até haver autenticação. No SISAN: **NÃO TRANSPORTAR** a implementação; preservar a capacidade com token por dispositivo — ver [`modulos/integracoes.md §4.2`](../modulos/integracoes.md) |
| 13 | **Artefato de relatório batch acessível sem controle de acesso** (elevado de "dúvida prioritária" a achado confirmado) | Cadeia completa: (a) `FiltroSSO:22` chama `getSession()` **criando** a sessão e as linhas 25-29 têm **os dois ramos do `if/else` idênticos** — o gate não condiciona nada; (b) `FiltroSessaoExpirada:38` só bloqueia se `sessao == null`, condição tornada **inalcançável** pelo filtro anterior; (c) `FiltroLimparSessao:96` sempre segue; (d) `FiltroSegurancaAcesso:279` — no ramo `usuarioLogado == null` chama `contemUrl()`, que em `:451-456` faz `url.contains(key) \|\| url.toLowerCase().contains(key)` sobre `urls_sem_usuario_na_sessao.properties:7` → `relatorio=relatorio`; `/exibirRelatorioBatchAction.do` casa em minúsculas; (e) `ExibirRelatorioBatchAction:40-52` localiza o `RelatorioGerado` **apenas pelo `idFuncionalidadeIniciada` do request**, sem comparar usuário, solicitante ou abrangência; (f) `GcomAction` não autoriza esse objeto | Leitura de relatório de terceiro por troca de identificador; alcançável **sem usuário autenticado** no nível da aplicação. Relatórios batch contêm dados de clientes (LGPD) | **P0\*** em instalação operante. No SISAN: artefato pertence ao solicitante, com verificação de propriedade e abrangência no download. *Ressalva operacional*: proxy/VPN/SSO externo pode reduzir a exposição de uma instalação, sem eliminar o defeito da aplicação |
| 14 | **Cadeia de filtros anteriores ao gate não barra nada** | `FiltroSSO:25-29` (ramos idênticos) + `FiltroSessaoExpirada:38` (guarda inalcançável) + `FiltroLimparSessao:96` (sempre segue) | O único controle efetivo para `*.do` é o `FiltroSegurancaAcesso`; os três filtros anteriores dão falsa sensação de defesa em profundidade | P1: no SISAN, cadeia de filtros com responsabilidade única e testada; **nenhum filtro decorativo** |
| 15 | **Entrada de dados de campo sem autenticação** | `ProcessarRequisicaoDipositivoMovelAction:67-116` (protocolo binário por opcode; `ATUALIZAR_MOVIMENTO` e `FINALIZAR_LEITURA` **gravam**) e `ProcessarRequisicaoTelemetriaAction:50-53` — nenhuma verificação de credencial; ambos listados como exceção em `FiltroSessaoExpirada:47-53` e `FiltroSegurancaAcesso:186-193` | Leitura de hidrômetro é insumo de consumo → faturamento. Injeção de leitura falsa é **fraude de faturamento** | **P0\*** em instalação operante. No SISAN: autenticação de dispositivo obrigatória — ver [`modulos/integracoes.md §3`](../modulos/integracoes.md) |
| 16 | Assinatura DSA do GIS sem prova de frescor | `ProcessarRequisicaoGisAction:162` + `AssinaturaDSA.validarHash:68-95` — assinatura `SHA1withDSA` cobre **apenas** `loginUsuario.getBytes()`: sem corpo, sem *nonce*, sem *timestamp* | Assinatura válida é portador estático, reutilizável indefinidamente (repetição) | P1: **MODERNIZAR** — o conceito está certo (autenticar a origem criptograficamente); falta expiração e escopo |
| 17 | Servlet com estado de instância | `OrdemServicoAPI:32-34` — `request`, `response` e `resposta` como campos de instância; servlet é singleton no container | Requisições concorrentes compartilham estado → resposta de um usuário pode alcançar outro | P1: **NÃO TRANSPORTAR**; no SISAN, componentes sem estado por requisição |
| 18 | Dado pessoal versionado | `ServicoSMS:67` — número de telefone real em `main()` | LGPD; dado pessoal no histórico do Git | P1: remover; massas de teste sempre anonimizadas |
| 19 | Integração UPA/SAM: falha silenciosa e escrita direta em banco de terceiro | `RepositorioIntegracaoHBM:88-108` (segunda `SessionFactory` via `HibernateUtil.getStatelessSessionIntegracaoSAM`, `session.insert`, `ConstraintViolationException` engolida) e `ControladorIntegracaoSEJB:238-248` (`continue` + `System.out` quando o login não resolve — OS **não** encerrada, sem registro durável) | Trabalho perdido sem rastro; acoplamento máximo ao schema alheio | P1: **REESTRUTURAR** — contrato explícito, erro durável e observável — ver [`modulos/integracoes.md §6`](../modulos/integracoes.md) |

### Defeito funcional correlato (não é segurança, registrado aqui por proximidade)

🟢 `ServicoSMS.getJson:41-58` **ignora o parâmetro `tipoMensagem`** e sempre monta o texto de confirmação de cadastro no Portal. `ControladorFaturamento:15268` solicita aviso de vencimento e o cliente recebe a mensagem errada. `ControladorCobranca:62373` tem o SMS de corte comentado (inativo). Registrado em [`modulos/integracoes.md §8.2`](../modulos/integracoes.md).

## Regras desde já

- Qualquer segredo encontrado em código, script ou configuração é considerado comprometido e entra na lista de rotação.
- Nenhuma melhoria pode reduzir controles existentes (expiração, bloqueio, histórico de senha, permissões especiais, abrangência, auditoria).
- Secrets do sistema novo: fora do código, por ambiente (variáveis/cofre), com contas de banco distintas por finalidade e menor privilégio no PostgreSQL.
